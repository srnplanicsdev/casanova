/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
   reactStrictMode: true,
  images: {
    domains: ['api.dicebear.com', "https://picsum.photos"],
  },
};

export default nextConfig;
